import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic_angular_sample',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
